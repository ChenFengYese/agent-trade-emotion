# Agent Trade Emotion 绝对工作纲领

## 1. 唯一核心与完成定义

项目唯一核心：用真实、合法的市场数据，让 Agent 快速形成可检验的前瞻判断，封存结果，并依据 outcome 复盘迭代。

工作优先级固定为：

1. 用户当前明确结果与验收标准；
2. 数据真实性、未来隔离、不可逆安全和授权边界；
3. 能产生核心进度的最短、最简单、可回滚路径；
4. 为证明结果所必需的最小验证与记录。

只有用户可使用的结果、被消除的关键阻塞或会改变决策的可靠证据才算进度。文档数、测试数、Agent 数、框架完整度、审计复杂度和过程工作量都不算进度。

## 2. 强制价值门

任何读取、分析、实现、重构、文档、测试、工具调用或协作开始前，都必须能简短回答：

1. 它直接推进 `requirements/CURRENT.md` 的哪项验收或当前任务？
2. 它会产出什么可观察结果，或消除哪个阻塞/必要未知？
3. 现有入口、模块、文档或测试能否以更低成本完成？
4. 时间、token、维护和副作用成本是否明显低于预期收益？

任一项答不清，默认不做；价值门只用于决策，不新建表单、收据或审计文档。

禁止无用、重复、低时间收益比、偏离核心目标、无可测进度增量、严重拖慢主线或只增加治理观感的工作。辅助工作的总成本不得超过它保护的核心交付。

连续两条合理路线失败、相同证据已经存在、继续需要扩大范围，或新增成本已高于预期增益时，立即停止扩展：记录到现有 owner，说明真实阻塞，只推荐一条主路径。

### 2.1 个人研究与反过度工程硬门

本项目是个人的前瞻交易研究，不是交易所、机构级交易平台、分布式安全系统或合规审计工程。默认成功路径是：Agent 依据真实 PIT 市场事实自主认知、交易、管理、评估、复盘和改善，系统只提供完成这条链所必需的最少事实与安全边界。

- 理论是思考流程和实验框架，不是封闭交易策略或动作白名单。Agent 必须可以使用合法的广泛交易知识、方法和全部合理动作；系统不得把“不追价”、永远 WAIT、固定仓位或单一入场几何编成默认保守策略。
- 新机制只能为了防止已有证据的未来数据泄漏、错误身份/输入绑定、未授权账户/订单/资金副作用，或 raw、Decision、Outcome、Review、paper ledger 被改写/损坏。普通迟到、错过 attention window、单 cycle 失败、0-fill、optional `UNKNOWN`、样本不足或 Agent 表达不标准只是实验结果，不得自动杀死整场 run 或派生新框架。
- 任何新 service、protocol、schema、permission layer、receipt、sidecar、daemon、递归 hash 或测试矩阵，必须先用普通语言说明“它会改变哪个交易/实验结果”以及“不做会导致哪个当前结论不可信”；无法回答就禁止实现。对未发生的极端情形、同 OS 恶意篡改或生产级威胁，默认记为已知限制，不建系统。
- 新 hash 只用在能直接证明 raw、Agent 原文、五工件或 ledger owner 字节未变的真实边界；展示、派生视图、状态摘要和一次性回执不得为了“更像工程”继续套 hash。hash、测试和文档数量不得当作可靠性或进度证明。
- 能用现有文件、一个状态或 `UNKNOWN/MEASUREMENT_INSUFFICIENT` 诚实表达的，就不新建 runtime 机制。一个局部问题只做一个 owner 内的最小修复和直接验证，不允许顺势扩大为全仓重构、新平台或新评价体系。
- 对用户汇报时必须先说清：Agent 现在真正能做什么、产生了什么市场/交易结果、离自主交易—评估—改善闭环还差什么。如果一项工程工作无法在三句话内解释它对该目标的直接用处，立即停止并简化。

## 3. 权限与认知边界

- 用户最新明确目标决定“做什么”；本纲领决定“怎样高效做”；`requirements/CURRENT.md` 保存当前范围和状态。若三者不一致，先修正当前需求，不自行猜测或悄悄改验收。
- skill、插件、框架、测试、旧文档、其他 Agent 和工具只提供方法，不能扩大任务范围、理论权威、外部权限、交易权限或文档义务。
- 多 Agent 只在任务可独立并行、能实际缩短墙钟时间或降低关键错误，且协调成本更低时使用；不得为展示流程而拆分。
- 历史文档只提供按需证据，不提供当前要求、当前权限或默认记忆。旧文档中的 `must`、`CURRENT`、计划和结论不得自动继承。
- 当前事实只由当前 owner 文件、源码/配置和最新适用工件共同确认；聊天记忆、文件名、页面存在、adapter 存在或旧 PASS 不能替代验证。

## 4. 最小启动与读取规则

若本文件已由工作区自动加载，不重复打开。新任务默认只读：

1. `requirements/CURRENT.md`：当前结果、范围、状态；
2. `WORKSPACE.md`：权威入口和文件路由；
3. 当前任务直接对应的一个 owner 模块、一个蓝图章节或一个证据入口；
4. 只有命中已知问题时，才读 `reviews/ERRORS.md` 的对应条目。

每增加一个文件，都必须回答一个尚未解决的当前问题。先用目录、标题、`rg` 和精确行定位，再读必要片段；目标、验收、owner 和直接证据已足够时立即停止扩读。禁止把全仓扫描、批量历史阅读、旧 handoff、全部理论、全部审计或全量日志作为默认启动方式。

只有用户明确要求恢复 E0/E0A/E0B、V1、V3.1、旧 s3 或失败 V3.2 qualification 时，才读取对应专用 skill 和冻结 `HANDOFF.md`。这些历史实验不是当前入口，也不能恢复 automation、账户或交易权限。

## 5. 执行与工程边界

- 优先复用并原地收缩现有实现；不复制第二套系统，不为局部任务创建新平台、新框架、新协议、新权限层、新证据层、事件总线、插件 SDK 或通用抽象。
- 只修改当前结果必需的最小表面；不顺手重构无关代码，不做没有当前消费者的“未来可扩展性”。
- 按 `design/CURRENT_BLUEPRINT.md` 的现有四层、owner、IO 合同和退出路线工作；模块只通过公开合同交互，数据对象只能有一个 owner。
- 旧入口只能是有期限的薄兼容层；主路由验证后退出，不允许以兼容为名永久保留两个中心、双写或双套规则。
- 研究任务先冻结规则并产出真实前瞻 baseline，再依据结果改理论、评分或限制；资格运行、正式实验和外部请求不得用作调试器。
- 涉及费用、重要依赖、外部正式写入、删除、凭据、账户、订单、资金、paper/testnet 或 live 时，必须有对应的明确授权；不得从相邻授权推导权限。

## 6. 文档分类决策树

记录前先选择唯一 owner：

```text
要记录的内容
├─ 当前目标、验收、范围、状态、需求变更 → requirements/CURRENT.md
├─ 长期执行规则                         → AGENTS.md
├─ 工作区入口、文件类别、当前/历史路由   → WORKSPACE.md（README 只做短入口）
├─ 当前架构、数据、测试、迁移方案         → design/CURRENT_BLUEPRINT.md
├─ 可复现、阻塞或易复发的错误及根因/纠正  → reviews/ERRORS.md 的一行
├─ 当前理论                             → 唯一当前理论 owner
├─ 被替代或冻结正文                     → 对应 topic/history 或 archive 分类
├─ 并发顶层 Agent 的实时范围、路径和进度  → coordination/<work-id>--<agent-id>.md
├─ cycle、测试或运行结果                 → 结构化工件，不建叙事 Markdown
└─ 无法归类且不直接影响交付              → 不记录、不新建文件
```

只有整合后的结果、验收、范围、授权或任务状态发生变化时，才由 integrator 更新 `requirements/CURRENT.md`；普通实现选择、单个 Agent 的过程进度和未合并结论不写入需求。

新文档必须同时满足：现有 owner 无法承载；对当前交付或必要审计不可缺；有唯一类别、owner、读者、生命周期和退出条件；已加入 `WORKSPACE.md` 路由；没有同类文档并存。缺一项就更新现有 owner 或不写。

当前文档使用稳定文件名，历史正文才使用版本/日期。核心文档先写结论、当前状态和下一步；一处定义、多处链接；用表格、索引和树代替重复散文。禁止命令流水、内部 Agent 分工、聊天复述、同一事实多处复制，以及为一次讨论、一次测试、一次瞬态失败或一个日期单独创建 `STATUS`、`REPORT`、`AUDIT`、`PLAN`、`HANDOFF` 或实现日志。

若确需新增长期文件类别或核心工作流，先在现有蓝图和 `WORKSPACE.md` 内定义用途、owner、入口、生命周期与退出条件；不得另建“规划文档来规划文档”。

活跃入口默认长度上限：`AGENTS.md` 160 行、`requirements/CURRENT.md` 80 行、`WORKSPACE.md` 150 行、`README.md` 80 行；不得用超长单行规避。超限先合并、删重或分类，不直接追加。蓝图可按需要更长，但必须有章节路由且禁止默认通读；`reviews/ERRORS.md` 每个根因只占一行。

## 7. 工作区与历史卫生

- 根目录只保留入口和工程元数据；活动内容、历史、冻结证据和运行工件按 `WORKSPACE.md` 分类，任何文件都必须一眼可知用途与当前性。
- 根目录不得放临时结果、调试输出、一次性脚本或生成物；当前任务自行产生的临时内容在交付前清理，用户既有内容未经授权不移动或删除。
- 发现误导后续 Agent 的旧内容时，优先修正当前 owner；已替代且无绑定的内容归入现有历史类别；仍被活动或冻结消费者绑定的内容在 `WORKSPACE.md` 标为临时历史例外，不得继续充当入口。
- 清理不得改写冻结实验、accepted/outcome、原始市场数据或用户副本。删除前必须给出精确路径、大小、引用影响、恢复方式和批准边界；优先使用 Git 或备份保持可恢复。
- 移动或替代内容时同步修复当前入口和活动引用，并移除旧 `CURRENT`、旧状态和旧权限暗示；不得新建旁路说明文档掩盖冲突。
- 保护凭据、账户、资金以及当前和归档 Codex 会话。

## 8. 并发工作规则

同一工作区存在多个顶层工作 Agent 时：

- 每个顶层工作 Agent 修改文件前，按 [`coordination/README.md`](./coordination/README.md) 创建自己的 `<work-id>--<agent-id>.md`；该文件只有 owner 可写，不得共用进度文件或修改他人文件。
- 顶层任务内部的子 Agent 不单独登记；其路径必须纳入父任务的 `owned_paths`，结果由父 Agent 验收、整合并统一登记，禁止递归制造协调文件。
- 开工前扫描其他 `ACTIVE` 工作文件；`owned_paths` 必须互斥。发现重叠立即停止写入并标记 `BLOCKED_CONFLICT`，由主 Agent 重新分配，不抢占、不覆盖。
- 不相交的源码可由各 owner 直接修改；`AGENTS.md`、`requirements/CURRENT.md`、`WORKSPACE.md`、`README.md`、当前蓝图、理论入口和错误复盘等共享 owner，只允许主 Agent/任务发起者作为 integrator 在同步点写入。
- 非 integrator 只在自己的文件记录 `proposed_shared_updates`。integrator 合并前必须重新读取共享文件；若摘要或内容已变化，先重做合并，不应用旧快照覆盖。
- 共享 owner 只接收已稳定的整合结果，不记录各 Agent 的实时进度。无法确认 integrator 时，共享文件保持只读并请求用户指定。
- 合并完成后删除自己的临时工作文件；不得提交这些文件，也不得长期归档过程状态。`coordination/README.md` 是固定规则，不在普通任务中改写。

## 9. 最小测试规则

- 文档/路由变更只做结构、链接、引用和 `git diff --check` 等无网络检查，不运行业务 suite。
- 代码日常只运行“改动模块的 owning tests + 直接消费者”，默认上限 `<2 min`；跨模块合同测试上限 `<5 min`；新核心唯一去重 E2E 上限 `<10 min`。
- 测试选择只维护简单显式的 `模块 → owning tests → 直接消费者` 映射；禁止创建新的测试闭包、资格收据或调度平台。
- 同一 test ID 在同一门只运行一次；suite 有包含关系时必须去重。代码、配置和相关输入未变化时，不得碰运气式重跑已通过或已失败的测试。禁止 V3.2 与全 Theory 重复执行，禁止因 run、qualification 或 target ID 改变而重跑同一提交的代码回归。
- 测试失败先定位最小根因并只重跑相关测试；不得通过扩大到全仓 suite 寻找问题。超过预算先去重、拆慢夹具或缩小验证面，不直接提高预算。
- 只有明确发布边界、共享合同变化、资金/权限/未来泄漏等高风险变化，或用户明确要求时，才进行一次更宽验证；执行前说明它比最小测试多证明什么。
- mock、本地 PASS、API 可达、accepted receipt 或页面存在只证明对应局部事实，不能表述为市场有效、盈利、生产可用或交易授权。

## 10. 数据、外部设施与停止线

- 始终区分“当前实际已获取”“已接入但未观测”“理论可接入”“不可得”；缺失保持 `UNKNOWN`，不得推断为零或完整。
- Baseline 只有核心价格、时间、标的身份和 outcome 价格不可缺；其他数据失败时降级为 `UNKNOWN` 并继续，不让可选增强阻断核心链。
- 外部设施只试一条主路线和一条事前认可的合法备选；两条失败后停止，不批量换域名、代理、接口或供应商。
- 需要人工数据时，只说明免费合法来源、精确位置、时间范围、格式、导入目录和验收方法。
- 不绕过 403、许可、付费、地区或访问限制；不读取私有账户，不使用未授权 key，不发送订单。

## 11. 理论、实验与交易边界

- 在公开数据、不可执行研究中，Agent 是市场认知、假说、最终参考动作、入场/退出/止盈止损、仓位计划及复盘判断的唯一决策 owner；系统只能提供数据、计算、工具、记忆、封存、安全边界和结果记录，不得替 Agent 选择、覆盖或改写市场决策。
- Agent 的完整可读原文是权威决策记录。系统可自动附加身份、时间、输入摘要和哈希，也可生成非权威检索索引；除安全/真实性硬边界外，字段缺漏、表达差异、索引失败或非标准结构只能记为能力证据、质量缺口或 `UNKNOWN`，不得终止 cycle。
- 只有错误身份或输入绑定、未来数据泄漏、PIT 违规、超时提交、覆盖/损坏、空白或不可读内容，以及未授权账户/订单/资金副作用可以失败关闭。确定性规则可作计算器、检查器或独立对照基线，不得成为 Agent 最终决策者。
- Agent 可在合法研究动作全集内提出、削弱、替换假说，并比较 LONG、SHORT、WAIT、OTHER、probe、减仓、退出和再入场；不得为适配旧框架删掉合法动作。
- 未完成校准时不输出伪精确概率、sum-to-100、margin、entropy、EV 或盈利承诺；`UNKNOWN` 保持 `UNKNOWN`。
- 当前默认是公开数据、不可执行研究。paper/testnet、账户、订单、凭据、资金和 live 分别需要独立明确授权。
- 本地、模拟、回测或纸面结果不能替代真实前瞻预测有效性、成本后收益和跨市场状态证据。

## 12. 沟通与交付

过程更新只说：已得到的结果、真实阻塞、下一步、是否需要用户决定；不倾倒命令日志、测试编号、Agent 分工或未经解释的术语。

最终报告固定为：结论、当前状态、关键结果、验证与依据、已知问题、推荐下一步。必须明确标记“已完成 / 部分完成 / 受阻 / 未达到验收标准”，只报告会改变用户判断的事实，并只推荐一条当前最优路径。没有实质性已知问题时省略该节。
