# 当前工作区整理与版本化提交

状态：已完成（随本版本化提交生效）

## 用户最终需要的交付结果

- 清理当前工作区的版本控制边界；
- 将已经完成的设计、合同、实现、测试与必要证据纳入一次可审计的本地 Git 提交；
- 对尚未完成、未获授权或尚未形成实验结论的模块作明确备注后一起提交；
- 提交后工作区保持干净，或只剩被明确忽略的本地运行时数据。

## 验收标准

1. 提交前锁定并报告 cwd、分支、基线 HEAD 与实际变更范围；
2. 不删除或覆盖用户已有工作，不把 `.runtime/`、虚拟环境、环境变量文件、缓存或凭据加入提交；
3. 完成大文件、敏感信息、生成物与未完成状态检查；
4. 更新当前状态与实验 handoff，使已完成能力和未完成模块可从版本库直接识别；
5. 使用声明支持的 Python 3.12 运行与风险相称的完整验证；
6. 明确暂存范围，创建本地提交并确认提交后的 Git 状态；
7. 不推送远端，不恢复 automation，不启动 paper/live/真实交易。

## 当前范围与明确不做

- 范围包括当前分支中尚未版本化的理论文档、配置/合同、Agent skills、V1/V2 代码、测试、审计与合法历史证据；
- `.runtime/` 保持 Git 忽略并保留在本机，不以“清理”为名删除；
- 不将尚未完成的 32 对原生 Codex 实验描述为完成；权威 practical run 保持 `completed=0 / next_sample=96`；
- 不修改冻结理论、实验阈值或历史结果来使验证通过；
- 不创建远端提交、PR 或发布标签。

## 当前主要任务

- 工作区与提交边界审查：**已完成；`.runtime/`、环境文件、缓存和构建目录保持忽略，Git 范围无大于 5 MiB 文件，常见凭据特征扫描无命中**；
- 未完成模块与恢复点备注：**已完成；见 `WORKSPACE_CHECKPOINT_2026-07-31.md`、`CURRENT_SYSTEM_STATUS.md` 与原生实验 handoff**；
- 全量验证：**已完成；Python 3.12 全套 `1164/1164 OK`，全部 JSON 解析与权威 run checkpoint/context/event 校验通过；staged whitespace 报告只含不能在本次任意改写的历史/冻结字节与既存 EOF 空行，已在工作区检查点显式记录**；
- 暂存与本地提交：**已完成；632 个非忽略变更路径作为同一工作区检查点提交，不含删除项**；
- 提交后干净状态确认：**已完成；版本化变更为零，`.runtime/` 等本地状态继续按 `.gitignore` 保留**。

## 需求变更记录

- 2026-07-31：用户要求先暂停实验，清理当前工作区，将已完成设计和实现提交；未完成模块须先备注再提交。
- 2026-07-31：全量验证首次发现旧 skill package 测试仍断言项目只能存在三个目录；已将其收紧为三个互斥决策角色包加一个非角色实验总控包，并通过全套 1164 项测试。该修复不改变冻结市场规则或实验结果。
