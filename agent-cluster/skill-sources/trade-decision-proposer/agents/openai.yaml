interface:
  display_name: "Trade Decision Proposer"
  short_description: "Build bounded multi-path E0 decision proposals"
  default_prompt: "Use $trade-decision-proposer to produce one bounded AgentProposalEnvelope from the supplied frozen Proposer view."
policy:
  allow_implicit_invocation: false
