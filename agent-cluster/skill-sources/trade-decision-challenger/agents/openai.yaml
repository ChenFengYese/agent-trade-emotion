interface:
  display_name: "Trade Decision Challenger"
  short_description: "Challenge frozen E0 proposals without veto power"
  default_prompt: "Use $trade-decision-challenger to produce one typed ChallengeEnvelope from the supplied frozen Challenger view."
policy:
  allow_implicit_invocation: false
