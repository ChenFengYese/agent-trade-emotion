interface:
  display_name: "Trade Bounded Selector"
  short_description: "Select only from a frozen feasible E0 action set"
  default_prompt: "Use $trade-bounded-selector to select one existing candidate from the supplied complete FeasibleActionSet."
policy:
  allow_implicit_invocation: false
